cmd 에서


mysql -u id -p

pass


SET GLOBAL event_scheduler = ON;

SET @@global.event_scheduler = ON;

SET GLOBAL event_scheduler = 1;

SET @@global.event_scheduler = 1;




cmd 또는 workbench 



CREATE EVENT del_play_log

    ON SCHEDULE

        EVERY 1 DAY

        STARTS CURRENT_TIMESTAMP

    DO

	DELETE FROM playlog WHERE Playtime < DATE_SUB(DATE_FORMAT(NOW(),'%Y-%m-%d %H:%i:%S'),INTERVAL 14 DAY);
